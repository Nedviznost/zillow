/* eslint-disable camelcase */
import { useEffect } from 'react'
import { useAppDispatch } from '..'

export const useFetchUserData = () => {
  const dispatch = useAppDispatch()

  // const [{ data, fetching, error, stale }] = useGetKorisnikQuery()
  // console.log(data)
}
